I combine the artifacts with equal paths from two snapshots to form a patch.

Instance Variables
	diffs:		<Dictionary> diffs in the resulting patch
	leftArtifacts:		<Dictionary>
	rightArtifacts:		<Dictionary>