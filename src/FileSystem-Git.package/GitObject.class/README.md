I am an abstract object that defines messages to which every object (in git speak) responds.

Instance Variables:
	repository <GitRepository>
		A private variable that a GitObject uses to lazily load itself